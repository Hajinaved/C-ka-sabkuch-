#include<stdio.h>
int main(){
	int a,b,s;
	printf("enter any two number for the values of a and b which are going to be swapped\n");
	scanf("%d%d",&a,&b);
	printf("you entered\na=%d\nb=%d\n",a,b);
	a=s;
	b=a;
	s=b;
	printf("swapped values are a=%d and b=%d\n",a,b);
	
}
