#include<stdio.h>
#include<conio.h>
int main(){
int x=0;/*here we initialize x as 0 so that it assigns itself as 0 if non integer input is given*/
scanf("%d",&x);
printf("\n %d\n",x);
getch();
return 0;

}